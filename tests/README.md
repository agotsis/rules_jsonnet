In order to build these examples, run from the repository top level:

`bazel build @examples://...`

The external directory is inteded to be used to make sure that dependencies on
 external repositories work properly.

